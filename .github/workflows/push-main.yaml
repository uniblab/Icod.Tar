name: build and test

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - run: dotnet clean Icod.Tar.sln -c Release
      - run: dotnet restore Icod.Tar.sln
      - run: dotnet build Icod.Tar.sln -c Release --no-restore -p:ContinuousIntegrationBuild=true
      - run: dotnet test Icod.Tar.sln -c Release --no-build --logger trx
